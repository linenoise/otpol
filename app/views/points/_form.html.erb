<%= form_for @point, :html => { :class => 'form-horizontal form-model' } do |f| %>

    <%= f.text_area :description, :class => 'text-field form-control new-point' %>

    <%= f.submit 'Share your light', :class => 'btn btn-primary' %>

<% end %>
