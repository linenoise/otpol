<div class="landing container">
	<div class="row">
		<div class="col-md-9">

			<div class="content-box">
			<%= render partial: 'points/new' %>
			</div>

			<% @points.each do |point| %>
			<%= render partial: 'points/point', locals: {point: point} %>
			<% end %>

		</div>
		<div class="col-md-3">

			<div class="content-box">
			TODO recent happenings here
			</div>

			<div class="content-box landing">
			<h3>Active today</h3>
			<ul>
			<%= render partial: "person", collection: @active_today %>
			</ul>
			</div>

			<div class="content-box landing">
			<h3>Newcomers</h3>
			<ul>
			<%= render partial: "person", collection: @newcomers %>
			</ul>
			</div>

		</div>
	</div>
</div>